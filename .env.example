# Mayday environment. Copy to .env.local (gitignored) and fill in what you use.
#
# The browser bundle reads NO environment variables: feature flags are URL switches
# (`?flag=elevenLabs`, `?fake=1`, `?debug=1`, `?guide=1`, src/flags.ts) and API keys never
# reach the client (docs/01 threat model). Never VITE_-prefix a key: Vite would inline it.
#
# The variables below are read by Node processes on the laptop only. Vite does not load
# .env files into vite.config.ts, so set the MAYDAY_* switches on the command line as
# documented in README.md, or export them in your shell.

# --- Dev server (vite.config.ts) ---------------------------------------------------------
# Serve plain http://localhost:5173 instead of self-signed https. Laptop only: phones need
# https for the camera. Usage: MAYDAY_HTTP=1 npm run dev
MAYDAY_HTTP=

# Use a trusted local CA via mkcert instead of the self-signed certificate. Asks for your
# macOS password once. Usage: MAYDAY_MKCERT=1 npm run dev
MAYDAY_MKCERT=

# --- ElevenLabs key proxy (src/voice/providers/devproxy.mjs, docs/09) --------------------
# Stand-in for the DigitalOcean /api/proxy function (docs/04 TODO 1) until it exists. With a
# key present, `npm run dev` and `npm run preview` mount the proxy at /api/proxy on their own
# origin, so the phone reaches it over the same https URL it loaded the page from. Without a
# key the mount is skipped and every flagged voice feature stays on its local stub. Turn the
# features on per session with `?flag=elevenLabs`, `?flag=dispatcherSim`, or both:
# `?flag=elevenLabs,dispatcherSim`.
# Standalone: node src/voice/providers/devproxy.mjs [port, default 8788]
ELEVENLABS_API_KEY=

# The coach's voice, by ElevenLabs voice id or by the name shown in your voice library
# ("Daniel", "Charlotte", a cloned voice you added). The proxy resolves it once against the
# account and serves it at /api/proxy/voice; the app asks for it while LAUNCH is on screen and
# shows no voice setting of its own. Unset, unmatched, or unreachable: Brian speaks (docs/09).
# The dispatcher voice (Sarah) is fixed: a second character on stage, not a preference.
ELEVENLABS_COACH_VOICE=

# The SIMULATED dispatcher agent (docs/04 TODO 3). Create it once per account with
# `node scripts/create-dispatcher-agent.mjs`, which prints this line. The proxy turns it into
# a signed session URL for the browser; the id itself never ships. Missing: the scripted
# dispatcher answers CALL 911 even with `?flag=dispatcherSim`.
ELEVENLABS_AGENT_ID=

# --- Scene model (src/ai/assess.ts, docs/04 items 4 and 7, docs/11) ----------------------
# One camera frame to a vision model at the start of triage, behind `?flag=sceneAssess`. The
# key stays in the proxy; the browser posts the frame to /api/proxy/vision/assess on its own
# origin. Two providers, both OpenAI-compatible, picked by whichever key is here. Compare them
# on a real photo with `node scripts/assess-frame.mjs photo.jpg [model]`. Neither key: the
# camera keeps its own on-device cues and nothing else changes.

# Gemini, the default and the sponsor track. Free key: https://aistudio.google.com/apikey
# GEMINI_VISION_MODEL defaults to gemini-3.6-flash; gemini-3.5-flash-lite is quicker and weaker.
GEMINI_API_KEY=
GEMINI_VISION_MODEL=

# Featherless, the fallback. Any vision model in the catalog works
# (featherless.ai/models?modalities=vision). FEATHERLESS_VISION_MODEL defaults to
# Qwen/Qwen2.5-VL-7B-Instruct; Qwen/Qwen3-VL-8B-Instruct or google/gemma-3-27b-it for a sharper run.
FEATHERLESS_API_KEY=
FEATHERLESS_VISION_MODEL=

# Force one provider for the TEXT routes (the intent router and the rewording): `gemini`,
# `featherless` or `xai`. Unset picks Gemini, then Featherless, then xAI, by whichever key is
# present. The camera frame resolves separately and never lands on a text-only model, so
# `MODEL_PROVIDER=xai` with a Gemini key beside it runs Grok for the text and keeps the frame
# on Gemini. `npm run dev` prints which model serves which route.
MODEL_PROVIDER=

# The same key and the same model also answer /api/proxy/intent/route, the intent router
# (src/ai/intent.ts, docs/04 item 8), behind `?flag=intentRoute`. No extra configuration.

# xAI, a third provider for the text routes: the intent router above and the rewording of one
# canonical line behind `?flag=narrationFlavor` (docs/04 items 5 and 8). HopHacks credits; key
# from console.x.ai. Picked when it is the only model key here, or with MODEL_PROVIDER=xai.
# XAI_MODEL defaults to grok-4.20-0309-non-reasoning. The scene frame has not been tried on it,
# so the frame route skips xAI: with an xAI key alone, `?flag=sceneAssess` does nothing and the
# camera keeps its own cues. To run Grok and the scene model together, keep a Gemini key too.
XAI_API_KEY=
XAI_MODEL=
